<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Medical.mapper.ParentMenuMapper">
<resultMap id="result" type="Medical.domain.ParentMenu">
    <result property="uid"      column="U_ID"></result>
     <result  property="code"     column="code"></result>
    <result property="name"     column="name"></result>
    <result property="path"     column="path"></result>
    <result property="status"   column="status"></result>

</resultMap>
    <select id="select" parameterType="Medical.domain.ParentMenu" resultMap="result">

        select *
        from
        sys_parent_menu


    </select>
</mapper>